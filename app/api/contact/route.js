import {db} from '@/lib/db';import nodemailer from 'nodemailer';export async function POST(req){try{const body=await req.json();if(!body.name||!body.email||!body.message)return Response.json({error:'Missing fields'},{status:400});const record={...body,createdAt:new Date(),status:'new'};await (await db()).collection('messages').insertOne(record);if(process.env.SMTP_HOST){const t=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||465),secure:true,auth:{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}});await t.sendMail({from:process.env.SMTP_FROM,to:process.env.ADMIN_EMAIL,replyTo:body.email,subject:`Evermoss enquiry from ${body.name}`,text:`${body.message}\n\nPhone: ${body.phone||'—'}\nEmail: ${body.email}`})}return Response.json({ok:true})}catch{return Response.json({error:'Unable to send'},{status:500})}}
