import { db } from '@/lib/db';
export async function POST(req){const p=await req.json();if(!p.code?.trim()||!p.name?.trim()||!p.description?.trim()||!Number(p.price)||!p.image)return Response.json({error:'Complete all required fields and upload an image'},{status:400});if(!p.image.startsWith('data:image/'))return Response.json({error:'Invalid image format'},{status:400});const product={code:p.code.trim(),name:p.name.trim(),slug:(p.slug||p.name).trim().toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''),description:p.description.trim(),customDesign:Boolean(p.customDesign),price:Number(p.price),image:p.image,updatedAt:new Date()};await (await db()).collection('products').updateOne({slug:product.slug},{$set:product,$setOnInsert:{createdAt:new Date()}},{upsert:true});return Response.json({ok:true})}
export async function DELETE(req){const {slug}=await req.json();await (await db()).collection('products').deleteOne({slug});return Response.json({ok:true})}
