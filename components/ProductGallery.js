'use client';
import { useState } from 'react';
import { ChevronLeft, ChevronRight } from 'lucide-react';
export default function ProductGallery({images=[],name}){const photos=images.filter(Boolean);const [active,setActive]=useState(0);if(!photos.length)return null;const change=(step)=>setActive(i=>(i+step+photos.length)%photos.length);return <div className="product-gallery"><div className="gallery-frame"><img src={photos[active]} alt={`${name} image ${active+1}`}/>{photos.length>1&&<><button className="gallery-arrow previous" onClick={()=>change(-1)} aria-label="Previous photo"><ChevronLeft/></button><button className="gallery-arrow next" onClick={()=>change(1)} aria-label="Next photo"><ChevronRight/></button><span className="gallery-count">{active+1} / {photos.length}</span></>}</div>{photos.length>1&&<div className="gallery-thumbs">{photos.map((image,i)=><button className={i===active?'active':''} key={image} onClick={()=>setActive(i)} aria-label={`View photo ${i+1}`}><img src={image} alt=""/></button>)}</div>}</div>}
