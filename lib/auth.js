import {SignJWT,jwtVerify}from'jose';const key=()=>new TextEncoder().encode(process.env.JWT_SECRET||'development-only-change-me');export async function session(){return new SignJWT({admin:true}).setProtectedHeader({alg:'HS256'}).setExpirationTime('7d').sign(key())}export async function authorized(req){try{await jwtVerify(req.cookies.get('evermoss_admin')?.value,key());return true}catch{return false}}
